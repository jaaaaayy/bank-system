"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const RegisterForm = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-xl text-center">Open an account.</CardTitle>
      </CardHeader>
      <CardContent>Register form</CardContent>
    </Card>
  );
};

export default RegisterForm;
